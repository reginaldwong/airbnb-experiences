import React from "react"

const Hero = () => {
    return (
        <section className="hero">
            <img src="src/assets/photo-grid.png" alt="photo grid" className="hero--photo"/>
            <h1 className="hero--title">Unforgettable activities hosted by locals</h1>
            <p className="hero--description">Join unique interactive activities led by one of a kind hosts—both in person and online.
            </p>
        </section>
    )
}

export default Hero;